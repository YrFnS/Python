# Write a Python program to convert the bytes in a given string to a list of integers.

text = b'Write a Python program to convert the bytes in a given string to a list of integers.'

print(list(text))
